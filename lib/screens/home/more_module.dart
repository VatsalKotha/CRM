import 'package:flutter/cupertino.dart';

class MoreModule extends StatelessWidget {
  const MoreModule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
